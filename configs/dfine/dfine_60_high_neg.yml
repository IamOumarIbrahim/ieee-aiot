# D-FINE-N Configuration: 60% Negative Frames (High-Negative Ratio)
# Architecture: D-FINE-N (Nano variant, HGNetv2-B0 backbone)
# Dataset: 2,401 positive frames, 3,602 negative frames (6,003 total)
# Target Hardware: RTX 4060 (8 GB VRAM) Hardware Adaptation Protocol

__include__: [
  './dfine_hgnetv2_n_coco.yml'
]

output_dir: ./output/dfine_60_high_neg

train_dataloader:
  dataset:
    ann_file: data/processed/RGB/coco/dfine/instances_train_60_high_neg.json
