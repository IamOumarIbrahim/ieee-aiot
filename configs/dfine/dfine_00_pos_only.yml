# D-FINE-N Configuration: 0% Negative Frames (Positive-Only Baseline)
# Architecture: D-FINE-N (Nano variant, HGNetv2-B0 backbone)
# Dataset: 2,401 positive frames, 0 negative frames (2,401 total)
# Target Hardware: RTX 4060 (8 GB VRAM) Hardware Adaptation Protocol

__include__: [
  './dfine_hgnetv2_n_coco.yml'
]

output_dir: ./output/dfine_00_pos_only

train_dataloader:
  dataset:
    ann_file: data/processed/RGB/coco/dfine/instances_train_00_pos_only.json
